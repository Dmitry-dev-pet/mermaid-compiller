• Цель: оставить единственный источник документации — Context7 (через MCP/HTTP), полностью отключив локальное чтение каталога `memory/mermaid-full-docs`.

План изменений:
1. server.py
   - Удалить функцию `_search_mermaid_docs_local` и все её вызовы.
   - В обработчике `/docs/search` оставить только обращение к `_search_mermaid_docs_via_context7`; если Context7 недоступен, возвращать пустой `results` без локального fallback.
   - При необходимости добавить явные сообщения об ошибке (например, лог или флаг в ответе), но API остаётся тем же: `{query, search_query, style_prefs, results}`.

2. Клиент (public/app.js)
   - Ничего менять не нужно, так как он уже работает только с `/docs/search` и не знает о локальном источнике.

3. Чистка
   - Удалить неиспользуемый импорт `DOCS_ROOT` и связанные константы/папки из server.py.

После этих правок Context7 становится единственным поставщиком snippet'ов, а если он не отвечает, пользователь увидит пустой список в UI.