Чтобы автофиксер работал надёжнее, введём спец. system prompt:

1. Добавить константу `STYLE_FIX_SYSTEM_PROMPT` и детализировать её: пояснить, что нужно только поправить синтаксис стилей, указать тип диаграммы и напомнить про ASCII символы.
2. В `callCliproxyApiFixStyle` формировать user message с явным указанием типа диаграммы + валидные/невалидные дивы, чтобы модель понимала контекст.
3. Передавать в helper тип диаграммы (например, `iteration.diagramType`) и, возможно, куски ошибок, чтобы LLM получал всё нужное.

После подтверждения изменим helper/вызовы и прогоняем lint.