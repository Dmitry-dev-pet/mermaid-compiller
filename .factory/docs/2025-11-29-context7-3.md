План действий:
1. В `createIterationEntry` добавить поле `baseStructureCode`.
2. При запуске стилизации сохранять в `styleIteration.baseStructureCode` текущий `parentIteration.activeCode` (очищенный вариант структуры).
3. `callCliproxyApiFixStyle` расширить: добавить явный текст «Не используй Context7/документацию» и принимать параметр `structureCode`. User prompt будет содержать исходную структуру плюс невалидный стилизованный код, чтобы LLM имел clean контекст.
4. В `handleStyleRequest` передавать `styleIteration.baseStructureCode` в автофикс, вместо стилизованного.
5. Прогнать `npm run lint`.
После подтверждения сделаю изменения.