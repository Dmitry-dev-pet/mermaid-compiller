Цель: при нажатии «Стилизовать» запрашивать документацию/подсказки, ориентированные именно на стили и темы, не переиспользуя структурный Context7 ответ.

1. Клиент (public/app.js)
   • Добавить helper `fetchStyleDocsContext(iteration)` — формирует запрос на `/docs/search` вида `"style tips" + preparedPrompt + diagramType`, отправляет его аналогично основному поиску, возвращает snippets/style prefs.
   • В `handleStyleRequest` перед вызовом LLM сначала выполнять `fetchStyleDocsContext`; если Context7 ничего не вернул, fallback на базовый контекст из структуры.
   • Обновить структуру итерации: стиль-итерации хранят свой `styleDocsContextText` и meta, чтобы в модалке «Context7» видно, что были два разных запроса.

2. composeFinalPrompt
   • Для стиля использовать `composeFinalPrompt(preparedPrompt, styleDocsText)` — таким образом стилизация получает актуальные snippets.

3. История
   • Обновить модальные окна: для стиль-итераций выводить два блока (структура/стиль) или помечать, какой Context7 использовался.

После утверждения: реализовать отдельный fetch, адаптировать стиль-флоу, обновить UI/историю.