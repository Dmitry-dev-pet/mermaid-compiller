Чтобы автофикс работал на чистом контексте:
1. В `callCliproxyApiFixStyle` убрать любые ссылки на Context7 (уже так), но также нужно передавать в него базовую версию диаграммы без новых docs. Будем хранить в стадии ссылку на исходную структуру (`iteration.activeCode` до стилизации) и в auto-fix отправлять только её, плюс список ошибок.
2. Для `styleIteration` запомнить `baseStructureCode` (копия родительской диаграммы) и передавать её в `callCliproxyApiFixStyle`, вместо `styledCode`, чтобы фиксер видел исходную структуру без доков.
3. В user prompt фикса добавить текст «Do NOT introduce new documentation context; only fix syntax». После утверждения уберём любые Context7 упоминания из auto-fix пайплайна.
